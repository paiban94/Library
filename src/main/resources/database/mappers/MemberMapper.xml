<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lib.fin.member.MemberDAO">


	<!-- 회원가입 -->
	<!-- reg_id / mod_id emp_no로 되어있어서 변경해야함, 다른컬럼들도 변경 필요 -->
<!-- 	<insert id="memJoin" parameterType="MemberVO">
	INSERT INTO MEM_INFO (EMP_NO, PW, NAME, EMAIL, BIRTH, PHONE, EMP_TEAM, EMP_POSITION, REMAIN_HOLIDAY, AUTHORITY, EMP_IN_DATE, EMP_OUT_DATE, REG_DATE, REG_ID, MOD_DATE, MOD_ID, USE_YN)
   	VALUES  (#{emp_no},#{password}, #{name}, #{email}, #{birth}, #{phone}, #{emp_team}, #{emp_position}, "15", "USER", #{emp_in_date}, #{emp_out_date}, SYSDATE(), #{emp_no}, SYSDATE(),#{emp_no},"y"); 
   	</insert> -->
   	
   		<insert id="memJoin" parameterType="MemberVO">
	INSERT INTO MEM_INFO (EMP_NO, PW, NAME, EMAIL, BIRTH, PHONE, EMP_TEAM, EMP_POSITION, REMAIN_HOLIDAY, AUTHORITY, EMP_IN_DATE, EMP_OUT_DATE)
   	VALUES  (#{emp_no},#{password}, #{name}, #{email}, #{birth}, #{phone}, #{emp_team}, #{emp_position}, "15", "USER", #{emp_in_date}, #{emp_out_date}); 
   	</insert>
   	
   	<!-- emp_no modal용 -->
   	<select id="getEmpNoModal" parameterType="MemberVO" resultType="memberVO">
   		SELECT * FROM MEM_INFO
   		WHERE EMP_NO = ${emp_no}
   	</select>
   	
<!--    	
   	로그인
	<select id="getMember" parameterType="MemberVO" resultMap="getMemberResult">
	 SELECT * FROM MEM_INFO
	 WHERE EMP_NO=${emp_no}
	</select> -->
	
	<!-- 공통코드 권한,부서,직급 -->
	<!-- <insert id="setMemberRole" parameterType="java.util.Map">
	INSERT INTO ST_STD_CD
	VALUES (#{grp_cd}, #{cd}, #{cd_nm})
	</insert>  -->
	
   	<!-- 로그인 -->
 <select id="getMember" parameterType="MemberVO" resultMap="getMemberResult">
    SELECT M.*,
           T.CD_NM AS EMP_TEAM,
           L.CD_NM AS EMP_POSITION,
           U.CD_NM AS AUTHORITY
    FROM MEM_INFO M
    LEFT JOIN ST_STD_CD T ON M.EMP_TEAM = T.CD AND T.GRP_CD = 'T001'
    LEFT JOIN ST_STD_CD L ON M.EMP_POSITION = L.CD AND L.GRP_CD = 'L001'
    LEFT JOIN ST_STD_CD U ON M.AUTHORITY = U.CD AND U.GRP_CD = 'U001'
    WHERE M.EMP_NO = #{emp_no}
</select> 


	<resultMap type="MemberVO" id="getMemberResult">
		<id column="EMP_NO" property="emp_no"/>
		<result column="PASSWORD" property="password"/>
  		<result column="NAME" property="name"/>
  		<result column="EMAIL" property="email"/>
  		<result column="BIRTH" property="birth"/>
		<result column="PHONE" property="phone"/>
		<result column="EMP_TEAM" property="emp_team"/>
		<result column="EMP_POSITION" property="emp_position"/>
		<result column="REMAIN_HOLIDAY" property="remain_holiday"/>
		<result column="AUTHORITY" property="authority"/>
		<result column="EMP_IN_DATE" property="emp_in_date"/>
		<result column="EMP_OUT_DATE" property="emp_out_date"/>
		<result column="REG_DATE" property="reg_date"/>
		<result column="REG_ID" property="reg_id"/>
		<result column="MOD_DATE" property="mod_date"/>
		<result column="MOD_ID" property="mod_id"/>
		<result column="USE_YN" property="use_yn"/>
		
		<collection property="comVOs" javaType="List" ofType="ComVO">
			<id column="GRP_CD" property="grp_cd"/>
			<id column="CD" property="cd"/>
			<result column="CD_NM" property="cd_nm"/>
		</collection>
		
	</resultMap>
	


</mapper>
