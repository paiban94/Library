<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.lib.fin.schedule.ScheduleDAO">
  <insert id="setScheduleAdd" parameterType="ScheduleVO">
		INSERT INTO SCHEDULE_BASE 
		values(0, '20231024001', #{schedule_start_time}, #{schedule_end_time}, #{schedule_title}, #{schedule_contents}, #{schedule_kind}, '20231024001', '20231024001', sysdate(), sysdate(), 'y');
   </insert>
   
   <select id="getSchedule" resultType="ScheduleVO">
   		SELECT * FROM SCHEDULE_BASE
   </select>
   
	<update id="setScheduleUpdate" parameterType="ScheduleVO">
		UPDATE SCHEDULE_BASE SET
  		EMP_NO=#{emp_no}, SCHEDULE_START_TIME=#{schedule_start_time}, SCHEDULE_END_TIME=#{schedule_end_time}, SCHEDULE_TITLE=#{schedule_title}, SCHEDULE_CONTENTS=#{schedule_contents}, SCHEDULE_KIND=#{schedule_kind},MOD_ID=#{mod_id},MOD_DATE#{mod_date}
  		WHERE SCHEDULE_NO=#{schedule_no}
	</update>

	<delete id="setScheduleDelete" parameterType="ScheduleVO">
			DELETE FROM SCHEDULE_BASE
			WHERE SCHEDULE_NO=#{schedule_no}
	</delete>

	
</mapper>